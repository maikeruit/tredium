# Инструкция

```bash
# Cборка проекта
composer install
npm install
npm run build 
```

```bash
# Sail и Миграция
./vendor/bin/sail up -d
./vendor/bin/sail artisan migrate --seed
```

# Проект будет доступен по адресу

## https://localhost
